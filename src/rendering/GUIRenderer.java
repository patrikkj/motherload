package rendering;

import javafx.scene.canvas.GraphicsContext;

public class GUIRenderer {
	private Camera camera;
	private GraphicsContext gc;

	public GUIRenderer(Camera camera, GraphicsContext gc) {
		this.camera = camera;
		this.gc = gc;
	}
	
	public void render() {
		
	}	
}
